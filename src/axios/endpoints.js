const products = "/products"
const productTypes = "/product-types"
const product = "/product"
const carTypes ="/car-types"
const car = "/car"
const home ="/product-type"

export {products, productTypes, product, carTypes, car,home}